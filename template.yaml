# This AWS SAM template has been generated from your function's configuration. If
# your function has one or more triggers, note that the AWS resources associated
# with these triggers aren't fully specified in this template and include
# placeholder values. Open this template in AWS Infrastructure Composer or your
# favorite IDE and modify it to specify a serverless application with other AWS
# resources.
AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: An AWS Serverless Application Model template describing your function.
Resources:
  FunctionOne:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: MyFunctionOne
      Handler: TEST_FILE1.lambda_handler  # Uses file1.py
      Code:
        S3Bucket: myproject-acc-dev
        S3Key: myfolder/deployment.zip
  FunctionTwo:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: MyFunctionTwo
      Handler: TEST_FILE2.lambda_handler  # Uses file2.py
      Code:
        S3Bucket: myproject-acc-dev
        S3Key: myfolder/deployment.zip